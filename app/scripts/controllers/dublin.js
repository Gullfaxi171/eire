'use strict';

/**
 * @ngdoc function
 * @name workspaceApp.controller:DublinCtrl
 * @description
 * # DublinCtrl
 * Controller of the workspaceApp
 */
angular.module('workspaceApp')
  .controller('DublinCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
